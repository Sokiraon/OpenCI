stages {
  build {
    env {
      BRANCH = input("Specify a branch:")
      DEBUG = confirm("Should be in debug mode?")
      API_VERSION = rawlist("Select an API version:", [0, 1])
      OPTIONS = checkbox("Add build options:", [ "upload_pgy", "unit_test" ])
    }
    sh "env"
  }
}